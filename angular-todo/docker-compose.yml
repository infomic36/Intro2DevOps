version: 3.3
services:
  # Builded service
  front: angular-todo
    build: ./front
  # From registry
  proxy:
    image: nginx
  # From registry
  back:
    build: ./back
    image: back:latest